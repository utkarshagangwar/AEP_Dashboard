# AEP Dashboard — Frontend

Documentation for this project now lives in a single file:
**[../README.md](../README.md)** (see §8 "Frontend detail" for everything
that used to be in this file — routing, API proxy layer, auth, state
management, components, styling, env vars).

Do not add a new standalone frontend doc here — extend the root README instead.
