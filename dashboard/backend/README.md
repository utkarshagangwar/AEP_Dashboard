# AEP Dashboard — Backend

Documentation for this project now lives in a single file:
**[../README.md](../README.md)** (see §7 "Backend detail" for everything
that used to be in this file — API surface, data model, services,
background jobs, Robot Framework execution, config/env vars, migrations).

Do not add a new standalone backend doc here — extend the root README instead.
