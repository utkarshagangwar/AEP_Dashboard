# # Database — use localhost (not postgres) since you're running outside Docker
# DATABASE_URL=postgresql://neondb_owner:REDACTED@ep-wild-silence-adry3yuy-pooler.c-2.us-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require

# # Redis — same reason, localhost
# CELERY_BROKER_URL=redis://localhost:6379/0
# CELERY_RESULT_BACKEND=redis://localhost:6379/0

# # JWT — same values as root .env
# JWT_SECRET_KEY=REDACTED
# JWT_ALGORITHM=HS256
# ACCESS_TOKEN_EXPIRE_MINUTES=15
# REFRESH_TOKEN_EXPIRE_DAYS=7

# # Admin seed
# FIRST_ADMIN_EMAIL=admin@aep.local
# FIRST_ADMIN_PASSWORD=REDACTED